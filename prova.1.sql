create database petshop;
use petshop;

create table colaborador(
primary key id,
nome varchar (50),
cpf varchar (50)
);

create table atividades(
primary key id,
prioridade varchar(50)
solicitacao e situacao varchar(50)
titulo varchar (50),
descricao varchar (50)
);







